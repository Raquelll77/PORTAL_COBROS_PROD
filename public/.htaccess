Options -MultiViews

RewriteEngine On
RewriteBase /PORTAL-COBROS/public/

# Si es un archivo o carpeta existente, no lo toques
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Si no, manda todo a index.php
RewriteRule ^ index.php [QSA,L]
